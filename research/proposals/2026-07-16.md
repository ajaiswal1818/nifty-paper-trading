# Weekly research report — 2026-07-16

First research cycle. Live window covered: 2026-07-15 only (platform went live mid-week).
Run note: this cycle fired on a Thursday; RESEARCH_RUNBOOK.md expects Sundays. Consistent with
the scheduler drift already noted in daily runs (evening/night cycles fired at ~14:05/14:25 IST
on Jul 15). Recommend checking the scheduler timezone/config on the run machine.

## 1. Data integrity

- `sessions_2026.csv` was missing the 2026-07-15 row: the evening run fired at 14:05 IST
  (before close) so the capture step never saw close data. **Backfilled from web sources:**
  `2026-07-15,24085.85,24052.05,24078.50,13.27,13.80,0`
  - Open 24,085.85 = NSE pre-open settle (Business Standard live blog; matches the night run's
    recorded `nifty_open`). The 11:05 morning snapshot had 24,068.0 — treated as an early
    intraday print, not the official open.
  - Close 24,078.50 (+26.45, +0.11%) — Business Standard close report.
  - VIX close 13.27 (BS/Indian Awaaz); one source (HDFC Sky) reported 13.14. Flagging the
    ~0.13 disagreement; immaterial at these levels (both ≤ spread_vix 16).
  - Morning score 0 = the live-recorded score from the 11:05 off-schedule morning run
    (hindsight-free, from state.json notes in commit 7355648).
- Jul 16 row is tonight's evening run's job — not backfilled here.
- Compliance gap (already fixed going forward): the Jul 15 morning run predated the
  `morning_score_today` capture rule (added later that day in commit 535eef6), so the score had
  to be recovered from run notes. No action needed; the rule now exists.
- `expiries.csv` populated through 2026-08-25 — fine.
- Equity curves: rows present for every run on both books; Jul 15 "evening" marks used 14:05
  intraday prices, not the close. Zero P&L impact (both books flat, no positions), but worth
  fixing the scheduler so evening runs mark actual closes.

## 2. Live scorecard (2026-07-15 → 2026-07-16)

| Strategy | Trades this week | Realized P&L | Equity | Max DD | W/L |
|---|---|---|---|---|---|
| v3-news-directional | 0 | ₹0 | ₹1,00,000 (flat) | 0% | 0/0 |
| v3i-intraday | 0 | ₹0 | ₹1,00,000 (flat) | 0% | 0/0 |

- One entry decision so far (Jul 15): score 0 vs threshold 2 → correctly no entry for both
  books. The day closed +0.11%; a directional entry would most likely have bled friction/theta.
- **v3 vs v3i divergences: none possible yet** — identical signals, no positions taken, so the
  exit policies have not yet been differentially exercised. The A/B has produced zero
  discriminating evidence to date.
- Live vs backtest expectation: nothing to compare (backtest also takes no trade on a score-0 day).

## 3. Signal audit (live-recorded scores only — n = 1)

Jul 15 components (recorded at the 11:05 morning run) vs the day's actual move (+0.11%):

| Component | Reading | Vote | Day outcome | Verdict |
|---|---|---|---|---|
| GIFT vs prev close | within ±0.3% | 0 | +0.11% | consistent (neutral) |
| US overnight (S&P) | +0.38% | 0 | +0.11% | consistent (neutral) |
| FII net cash | −₹3,062 Cr | −1 | +0.11% | miss (marginal) |
| News sentiment | mixed | 0 | +0.11% | consistent (neutral) |
| PCR | 1.23 | +1 | +0.11% | hit (marginal) |

n = 1 with a ±0.11% day: zero statistical content. Running hit-rate tracking starts from this
baseline; meaningful reads need ≥ 20 scored mornings (~4 weeks).

## 4. Bounded parameter sweeps

One-at-a-time sweeps inside `tunable_bounds`, windows = (a) all data 2026-05-15→07-15,
(b) last 4 weeks 2026-06-18→07-15. Base results: v3 +5.3% all / +3.7% last4w (9 exits);
v3i +23.0% / +19.3% (14 exits). Full grids reproducible via `engine/backtest.py <params.json>`
with the single parameter overridden.

Observations that pass the "improves both windows" screen but FAIL the sample gate:

| Strategy | Param | Base → Swept | All data | Last 4w | Affected live trades | Verdict |
|---|---|---|---|---|---|---|
| v3 | stop 0.35 → 0.25 | +5.3% → +10.8%, DD −28.9 → −23.7 | better | +3.7% → +12.5%, DD −27.3 → −14.3 | **0** (live), ~2 backtest exits changed | insufficient evidence — no action |
| v3 | target 1.50 → 1.30 | +5.3% → +13.6%, DD −18.8 | better | +3.7% → +14.8% | **0** (live), ~3 backtest exits changed | insufficient evidence — no action |
| v3/v3i | gap_chase 0.009 → 0.0125 | v3 +6.6%, v3i +30.3% | better (all) | unchanged both | **0** (live); fails "improves BOTH windows" (last4w flat) | no action |

- v3i is almost completely insensitive to stop/target/trail inside bounds (intraday holding
  period rarely reaches them before the EOD close) — its edge/noise lives entirely in entry
  selection and the EOD rule itself, which is exactly what the live A/B is testing.
- Overfitting note (applies to every row above): all "improvements" come from re-fitting 9–14
  backtest exits on data that includes the May regime the params were already tuned on. Rule 3
  gate: < 30 affected live trades → default verdict **insufficient evidence**. Rule 5 split-sample
  check also fails trivially (no untouched later window exists yet).

**Proposals this week: NONE.**

## 5. Candidate strategies

No systematic pattern can exist in 1 live day / 0 trades. No candidate drafted.

## 6. What would make next week's evidence stronger

1. Fix the run-machine scheduler (evening runs must fire after 15:30 IST close; research run on
   Sundays). Bad capture timing is currently the platform's only active defect.
2. Let the daily runs accumulate: every morning score (trade or no-trade) feeds the signal
   hit-rate table; every close feeds sessions_2026.csv.
3. First interesting checkpoint: the first v3-vs-v3i divergent day (an overnight hold that gaps),
   which is the whole point of the A/B.
